name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  lint:
    name: Lint
    uses: ./.github/workflows/lint.yml

  bundle:
    name: Bundle
    needs: lint
    uses: ./.github/workflows/bundle.yml
